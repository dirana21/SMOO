<Window x:Class="Task2.Views.GameView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Task2.ViewModels"
        xmlns:utils="clr-namespace:Task2.Utils"
        Title="2048" Height="560" Width="460"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        ResizeMode="CanResize"    
        Background="#F3EFE2"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <utils:TileColorConverter x:Key="TileColorConverter"/>
    </Window.Resources>
    <Window.DataContext>
        <vm:GameViewModel/>
    </Window.DataContext>

    <Grid Margin="16" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Border Width="200" Margin="8" Background="#D6CCB1" Padding="12" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Score" HorizontalAlignment="Center" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Score}" HorizontalAlignment="Center" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                </Border>

                <Border Width="200" Margin="8" Background="#D6CCB1" Padding="12" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="High Score" HorizontalAlignment="Center" FontWeight="Bold"/>
                        <TextBlock Text="{Binding HighScore}" HorizontalAlignment="Center" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <Button Content="New Game" Margin="8" Padding="16,10" Click="NewGame_Click" DockPanel.Dock="Right"/>
        </DockPanel>

        
        <Grid Grid.Row="1">
            <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Width="720" Height="720" Background="#E6DAC2" Padding="16" CornerRadius="12">
                    <ItemsControl ItemsSource="{Binding Cells}">
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                <Setter Property="VerticalAlignment" Value="Stretch"/>
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="4" Columns="4"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="8" CornerRadius="10"
                                        Background="{Binding Converter={StaticResource TileColorConverter}}">
                                    <Viewbox Stretch="Uniform">
                                        <TextBlock Text="{Binding}"
                                                   FontWeight="Bold"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Foreground="White"/>
                                    </Viewbox>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>
            </Viewbox>
        </Grid>
    </Grid>
</Window>
